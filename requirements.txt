# YouTube Data API
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.1.0

# Environment variable loading
python-dotenv>=1.0.0

# Data handling
pandas>=2.0.0
openpyxl>=3.1.0
tqdm>=4.65.0

# Sentiment — lexicon based (lightweight, no GPU needed)
vaderSentiment>=3.3.2

# Deep learning models (optional but recommended)
transformers>=4.38.0
torch>=2.1.0

# Visualization — publication-quality figures (300 dpi PNG / vector PDF)
matplotlib>=3.8.0
seaborn>=0.13.0
scipy>=1.11.0
